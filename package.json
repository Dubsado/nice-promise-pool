{
  "name": "nice-promise-pool",
  "version": "1.0.2",
  "module": ".lib/pool.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "^0.5.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "NODE_ENV=test bun test",
    "test:w": "NODE_ENV=test bun test --watch",
    "test:wl": "LOG_LEVEL=verbose NODE_ENV=test bun test --watch",
    "build": "bun build ./lib/pool.ts --outdir ./dist/promisePool.js"
  },
  "bin": {
    "promisePool": "./dist/promisePool.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Dubsado/promise-pool"
  },
  "directories": {
    "lib": "./lib"
  }
}
